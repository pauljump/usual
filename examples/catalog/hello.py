#!/usr/bin/env python3
"""A deliberately tiny contributor example, not a maintained menu item."""
import json
print(json.dumps({"outcome": "Hello from a complete, independently invocable example.",
                  "storage": "none", "network": False, "source": "https://github.com/pauljump/usual"}))
